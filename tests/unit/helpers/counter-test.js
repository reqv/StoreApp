
import { counter } from 'store-app/helpers/counter';
import { module, test } from 'qunit';

module('Unit | Helper | counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = counter([42]);
  assert.ok(result);
});

