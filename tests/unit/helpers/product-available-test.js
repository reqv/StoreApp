
import { productAvailable } from 'store-app/helpers/product-available';
import { module, test } from 'qunit';

module('Unit | Helper | product available');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productAvailable([42]);
  assert.ok(result);
});

